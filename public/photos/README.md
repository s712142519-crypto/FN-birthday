# Local Photos Folder

Place your custom birthday photos and lock screen photos inside this folder!
Once you upload your image files (JPG, PNG, WebP) here, you can reference them in `src/config.ts`.

For example, if you place a file named `my_photo.jpg` in this folder, you can reference it in your config as:
`/photos/my_photo.jpg`
