# Local Videos Folder

Place your custom MP4 files inside this folder!
Once you upload your video file (e.g., `intro.mp4`), you can change the config in `src/config.ts` to reference it.

For example:
`filePath: "/videos/intro.mp4"`
