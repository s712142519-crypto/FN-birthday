# Local Songs Folder

Place your custom MP3 files inside this folder!
Once you upload your audio files here, you can name them to match the `filePath` in `src/config.ts`.

For example, if you have a file named `lovin_life.mp3`, rename it and put it in this folder so it can be loaded at:
`/songs/lovin_life.mp3`
